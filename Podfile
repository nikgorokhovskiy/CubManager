# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CubManager' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CubManager

  pod "FlagPhoneNumber"
  pod "Firebase"
  pod "Firebase/Auth"
  pod "Firebase/Firestore"
  pod "SwiftEntryKit", "1.2.3"

end
